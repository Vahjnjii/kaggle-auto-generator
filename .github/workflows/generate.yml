name: Auto Generate Images

on:
  push:
    paths:
      - 'prompts.txt'
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - uses: KevKibe/kaggle-script-action@v1.0.5
        with:
          username: ${{ secrets.KAGGLE_USERNAME }}
          key: ${{ secrets.KAGGLE_KEY }}
          title: "Auto Image Generator"
          code_file: "notebook.py"
          language: "python"
          kernel_type: "script"
          is_private: "true"
          enable_gpu: "true"
          enable_internet: "true"
          sleep_time: 60
      
      - run: echo "✅ Done! Download from Kaggle"
```

4. Click: **"Commit new file"**

---

## STEP 7: Test It (Manual Trigger)

1. Click: **"Actions"** tab (top menu)
2. Click: **"Auto Generate Images"** (left sidebar)
3. Click: **"Run workflow"** (right side, dropdown button)
4. Click: **"Run workflow"** (green button)

**Wait 2-3 minutes** while it runs.

---

## STEP 8: Download Your Images

1. **While it's running**, open new tab: https://www.kaggle.com/code
2. Find notebook named: **"Auto Image Generator"**
3. Click on it
4. **Wait for it to finish** (watch the progress)
5. Go to **"Output"** tab
6. **Download the ZIP file**

✅ **Done! You have your images!**

---

## 📝 HOW TO USE DAILY

### To generate new images:

1. Go to your GitHub repository
2. Click `prompts.txt`
3. Click pencil icon ✏️ (Edit)
4. Change prompts:
```
a cat sitting on windowsill
a dog playing in park
a bird flying in skyY
